# TODO - Features to add

1) Command usage --  help <command>
Each command should implement a command usage message. This should be simple to add.
2) Integrated Game? Snake? Pong? Should be easter eggs.
3) What is fortunes in the share/games folder..? Cant use it.
4) projects simlink works, but exuecting the actual var/www/projects doesnt display anything?
5) /usr/bin should actually house the main commands.
6) I feel project_x's README should outline some notes one discovering the solution to time travel, then make them unable to access actual file... so readme should be a journeal entry somehow but still in style of a readme?
7) In hacker sequence, loading bar should stay on one line. It can be SLIGHTLY faster playing each line too.
8) Add reboot command (it can be aliased to the exit command without exit message?) Make sure these share code.
9. First two lines in sudo command should be instant, rest can be played out, but slightly faster than they currently are.



OTHRE BIG ONE:
1. Command to give a general run through this should be advertised for non-tech people to be able to use.

AFTER GOOD WITH TERMINAL
1. For non mobile, the terminal should be INSIDE of a retro poc displayed on the site possible.
